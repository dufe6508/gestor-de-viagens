import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestordeexcursoes.app',
  appName: 'Gestor de Excursões',
  webDir: 'out'
};

export default config;
