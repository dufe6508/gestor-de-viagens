import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestordeexcursoes.app',
  appName: 'Gerenciador de Viagens',
  webDir: 'out'
};

export default config;
